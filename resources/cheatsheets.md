# Git

git status
Shows changed files.

git add file
Stages one file.

git commit -m ""
Creates a commit.

git push
Uploads commits.

git pull
Downloads changes.

git checkout branch
Switches branches.